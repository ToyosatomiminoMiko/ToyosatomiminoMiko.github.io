{
  "name": "toyosatomiminomiko.github.io",
  "version": "0.0.0",
  "private": true,
  "description": "",
  "type": "module",
  "dependencies": {
    "bootstrap": "^5.3.8",
    "katex": "^0.18.1",
    "mathjs": "^15.2.0",
    "three": "^0.185.1",
    "vue": "^3.5.40"
  },
  "devDependencies": {
    "@types/three": "^0.185.3",
    "typescript": "^7.0.2",
    "vite": "^8.2.0",
    "vitest": "^4.1.10",
    "vue-tsc": "^3.3.9"
  },
  "scripts": {
    "dev": "vite",
    "build": "npm run build:wasm && npm run typecheck && vite build",
    "build:wasm": "cd math-lab/src/math/math_rs && wasm-pack build --target web --out-dir ../../wasm/math_rs && cd ../../compiler/compiler_rs && wasm-pack build --target web --out-dir ../../wasm/compiler_rs && cd ../../render/render_rs && wasm-pack build --target web --out-dir ../../wasm/render_rs",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  }
}
